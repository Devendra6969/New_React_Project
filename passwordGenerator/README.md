# 🔐 Password Generator (React)

A simple **Password Generator** built using **React** and **Tailwind CSS**.  
This project demonstrates the practical use of core React hooks like  
`useState`, `useCallback`, `useEffect`, and `useRef`.

---

## 🚀 Features

- Generate random passwords
- Adjustable password length using a slider
- Option to include:
  - Numbers
  - Special characters
- Copy password to clipboard
- Auto-regenerates password when options change
- Clean UI with Tailwind CSS

---

